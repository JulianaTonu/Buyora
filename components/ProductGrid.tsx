"use client"

import React from 'react';
import HomeTabBar from './HomeTabBar';

const ProductGrid = () => {
    return (
        <div>
            <HomeTabBar/>
        </div>
    );
};

export default ProductGrid;